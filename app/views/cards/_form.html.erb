

<% form_for [@deck, @card] do |f| %>
  <!-- if you add AR validations, you can explain here why something cannot be created -->

  <%= f.label :question %>
  <%= f.text_area :question %>

  <%= f.label :answer %>
  <%= f.text_area :answer %>

  <%= f.submit %>

<% end %>
